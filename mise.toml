[tools]
python = "3.11"
node = "22"

[env]
# Prefer local virtualenv for python deps in this repo.
VIRTUAL_ENV = "{{config_root}}/venv"
_.python.venv = { path = "{{config_root}}/venv", create = true }
